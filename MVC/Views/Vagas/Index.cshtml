@model IEnumerable<DAL.Models.Vaga>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ocupada)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ocupada)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VagaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VagaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VagaID })
        </td>
    </tr>
}

</table>
